# ids/

Pendente. Vai conter o `Dockerfile` do container `ids-1` (Suricata) e as
regras customizadas pra detectar os ataques GOOSE do `atacante/`
(flooding, replay, masquerade, suppression) — ver `docs/metodologia.md`,
Seção 5.
